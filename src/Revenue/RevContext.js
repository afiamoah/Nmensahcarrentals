import { createContext } from "react";
export const RevContext=createContext();
export const waitcontext=createContext(true)
export const AllDataContext=createContext([])
export const firstdate=createContext()
export const seconddate=createContext()
export const Categories=createContext()
